import PrizePoolClient from './PrizePoolClient';

export const metadata = {
  title: 'Prizepool | Tiers NB Esport',
  description: 'Cagnotte communautaire pour booster la compétition.',
};

export default function PrizepoolPage() {
  return <PrizePoolClient />;
}
