[build]
  publish = "public"
  functions = "netlify/functions"

# Les fonctions doivent être accessibles via un alias public
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Redirection SPA pour le reste
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
